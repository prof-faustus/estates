{
  "name": "@estates/channel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ESTATES mutually-authenticated encrypted IP-to-IP session (BRC-103 style): identity-key auth + ephemeral ECDH session + authenticated framing, on the in-tree crypto core (@estates/keys). secp256k1 ONLY (NO Ed25519), NO third-party library.",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@estates/keys": "workspace:*"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types \"test/**/*.test.ts\""
  }
}
