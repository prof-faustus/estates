{
  "name": "@estates/chainmap",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Bridge between the pure engine's title state and on-chain 1-sat NFTs: maps engine titles to NFT outputs and emits the re-mint / payment tx legs for build/mortgage/buy actions.",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@estates/params": "workspace:*",
    "@estates/engine": "workspace:*",
    "@estates/onchain": "workspace:*",
    "@estates/bank": "workspace:*"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types \"test/**/*.test.ts\""
  }
}
