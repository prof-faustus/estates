{
  "name": "@estates/keylife",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ESTATES one-game key lifecycle: a signed genesis key manifest binding EVERY key (seat/card/holder/chat/bank/settlement/trade) to a single game_id, with cross-game reuse rejection. A key is valid for at most one game.",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@estates/channel": "workspace:*",
    "@estates/keys": "workspace:*"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types \"test/**/*.test.ts\""
  }
}
