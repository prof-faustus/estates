{
  "name": "@estates/spv",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ESTATES native SPV (C3): verify tx inclusion by recomputing the Merkle root from a partial branch against a block header — no trusted full node, no third-party REST. Isomorphic (@noble).",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@noble/hashes": "1.5.0"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types \"test/**/*.test.ts\""
  }
}
