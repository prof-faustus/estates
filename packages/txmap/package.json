{
  "name": "@estates/txmap",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ESTATES every-move-on-chain mapping: each engine transition emits a BSV tx with an on-chain action commitment (pushdata, no OP_RETURN) + value legs to one-use keys, sat-conserving. Whole sats.",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@estates/engine": "workspace:*",
    "@estates/onchain": "workspace:*",
    "@estates/chainmap": "workspace:*"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types \"test/**/*.test.ts\""
  }
}
