{
  "name": "@estates/engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ESTATES pure deterministic game-rules core: turn FSM, landing resolution, economy. No I/O, no clock, no own randomness (dice + deck order injected, beacon-verifiable).",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@estates/params": "workspace:*"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types \"test/**/*.test.ts\""
  }
}
