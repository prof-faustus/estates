{
  "name": "@estates/bot",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ESTATES bot policies (cautious/balanced/aggressive) as turn-driver Deciders. TEST-ONLY: these policies exist to drive the engine in the test suite. NO bot ever runs in the live game — a human selects every action. There is no daemon and no networked bot (that would be both a running bot AND a server).",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@estates/params": "workspace:*",
    "@estates/engine": "workspace:*",
    "@estates/turn": "workspace:*",
    "@estates/trade": "workspace:*"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types \"test/**/*.test.ts\""
  }
}
