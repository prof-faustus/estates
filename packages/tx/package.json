{
  "name": "@estates/tx",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ESTATES canonical BSV transaction serialization + txid (whole-sat, bigint-safe). Pure/isomorphic; gives every on-chain move a real txid an SPV proof can reference.",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@noble/hashes": "1.5.0"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types \"test/**/*.test.ts\""
  }
}
