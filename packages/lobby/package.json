{
  "name": "@estates/lobby",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ESTATES lobby/waiting room — join, ready, start-authority, override-start; fixes network mode at genesis and auto-funds on regtest. Emits the EngineConfig that seeds the core.",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@estates/params": "workspace:*",
    "@estates/engine": "workspace:*"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types \"test/**/*.test.ts\""
  }
}
