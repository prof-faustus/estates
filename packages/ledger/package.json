{
  "name": "@estates/ledger",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ESTATES on-chain move chain: genesis + every move is a real linked BSV tx (each spends the prior move's commitment), so the txid sequence is the authoritative on-chain transcript.",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@estates/tx": "workspace:*",
    "@estates/txmap": "workspace:*",
    "@estates/engine": "workspace:*",
    "@estates/chainmap": "workspace:*"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types \"test/**/*.test.ts\""
  }
}
