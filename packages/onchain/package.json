{
  "name": "@estates/onchain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ESTATES on-chain objects (C5): BSV script model (OP_RETURN-banned at serialize), 1-sat title-deed + Reprieve NFTs (state as pushdata + OP_DROP), native-sat payments, provenance. Built to tx-nft doc §3 (absent in bsv-poker).",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@estates/params": "workspace:*"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types \"test/**/*.test.ts\""
  }
}
