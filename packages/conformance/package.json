{
  "name": "@estates/conformance",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ESTATES conformance vectors — the legality source of truth. (state, action) -> (next-state hash | typed rejection), generated by executing the core.",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@estates/engine": "workspace:*",
    "@estates/params": "workspace:*"
  },
  "scripts": {
    "generate": "node --experimental-strip-types src/generate.ts",
    "test": "node --test --experimental-strip-types \"test/**/*.test.ts\""
  }
}
