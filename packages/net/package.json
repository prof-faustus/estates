{
  "name": "@estates/net",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ESTATES peer transport (C7): an opaque relay contract (HTTP+SSE-style fan-out), an in-memory relay, and a deterministic peer session. Relay is untrusted; canonical state is the engine-replayed transcript. Reconnection replays history.",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@estates/params": "workspace:*",
    "@estates/engine": "workspace:*",
    "@estates/beacon": "workspace:*",
    "@estates/conformance": "workspace:*",
    "@estates/audit": "workspace:*"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types \"test/**/*.test.ts\""
  }
}
