{
  "name": "@estates/root",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ESTATES — a dealerless, peer-to-peer (NO server), real-BSV property game. Original non-copyright content; money in native satoshis; title/Reprieve cards as 1-sat NFTs (concealed deck cards via @estates/cardnft). On-chain settlement + audit are being completed; nothing is claimed complete that is not.",
  "license": "MIT",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": "9.0.0"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --experimental-strip-types \"packages/**/test/**/*.test.ts\"",
    "lint:bans": "node --experimental-strip-types tools/lint-bans.ts",
    "ci": "pnpm install --frozen-lockfile && node --experimental-strip-types tools/ci.ts",
    "reproduce": "pnpm install --frozen-lockfile && node --experimental-strip-types tools/ci.ts && pnpm --filter @estates/conformance generate",
    "redteam": "node --experimental-strip-types tools/redteam.mjs"
  },
  "devDependencies": {
    "@types/node": "22.19.19",
    "typescript": "5.9.3"
  }
}
