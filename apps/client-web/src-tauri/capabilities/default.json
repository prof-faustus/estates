{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "ESTATES desktop: allow the app to open additional windows for simulated players (each is a separate remote participant joined over the relay).",
  "windows": ["main", "bot-*"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:window:allow-create",
    "core:webview:default",
    "core:webview:allow-create-webview-window"
  ]
}
